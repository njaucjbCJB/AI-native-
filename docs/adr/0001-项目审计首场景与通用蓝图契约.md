# 项目审计作为首个场景，底层采用通用且版本化的业务蓝图契约

第一阶段只保证项目审计能够从业务描述生成表单、权限和审批流程并端到端运行，但项目审计字段和规则不得写死在界面或运行时中。系统采用领域无关且版本化的 `Blueprint` 契约，由确定性的项目审计生成器产生首个可部署蓝图；以后接入真实 AI 或扩展其他业务场景时，应替换或新增生成器，而不是重写表单、权限和流程运行时。已部署版本不可直接覆盖，既有审计实例始终绑定创建时使用的蓝图版本。
