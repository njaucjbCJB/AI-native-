# 业务运行蓝图作为唯一中心契约

状态：已接受

系统从“表单和审批流生成器”升级为“业务目标驱动的 AI-native 企业业务运行框架”。我们决定以 `Business Operating Blueprint` 作为唯一中心契约，统一承载业务目标、任务分发、自动执行、协同流程、权限规则、AI 验收、审计证据和报表规则；表单和审批流只是协同流程路径下的派生产物。这样可以避免 Skill 配置、表单配置、审批配置和报表配置各自分裂，确保自动执行与人工协同最终都沉淀为可审计、可验收、可报表化的业务实例。
